import { motion } from 'framer-motion'

export default function NarrativeSummary({ summary }) {
  if (!summary) return null

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 0.6, duration: 0.5 }}
      className="bg-slate-800/50 rounded-2xl border border-slate-700 p-8"
    >
      <h3 className="text-2xl font-bold text-white mb-4">
        📖 What Patients Reported (AI Summary)
      </h3>

      <motion.div
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.7 }}
        className="bg-indigo-900/20 rounded-lg p-6 border-l-4 border-indigo-500"
      >
        <p className="text-slate-200 leading-relaxed text-lg">
          {summary}
        </p>
      </motion.div>

      <p className="text-xs text-slate-400 mt-4">
        Summary generated by Claude AI based on FAERS patient narratives
      </p>
    </motion.div>
  )
}
